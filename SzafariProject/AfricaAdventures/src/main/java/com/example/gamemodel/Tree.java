package com.example.gamemodel;

public class Tree extends Plant {
    public Tree(Coordinate coord, double price, int quantity, GameModel model) {
        super(coord, price, quantity, model);
    }
}